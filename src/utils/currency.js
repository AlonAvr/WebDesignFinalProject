// Placeholder currency formatting helpers.
// Actual conversion logic will be implemented alongside the cost manager functionality.

/**
 * Formats a numeric amount with a currency code for display purposes.
 * This is a placeholder used only for shell/UI development.
 */
export function formatCurrency(amount, currencyCode = 'USD') {
  return `${amount} ${currencyCode}`
}
